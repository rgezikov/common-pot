# Phase 1 — Definition of Done

- [x] Django project created and runs locally with `python manage.py runserver`
- [x] Django app `pots` created within the project
- [x] SQLite database configured and migrations run successfully
- [x] Data models defined: `Pot`, `Member`, `Drop`, `Split`
- [x] All models registered and visible in Django admin
- [x] HTMX and TailwindCSS loaded via CDN in the base template
- [x] Base template renders correctly in a mobile browser
- [x] URL routing skeleton in place (placeholder views for main pages)
- [x] Project runs on the server via gunicorn behind nginx
- [x] Static files served correctly in production
