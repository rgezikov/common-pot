# Phase 1 — Project Foundation

> Set up the development environment and skeleton application.

## Tasks

- Initialize Django project and app structure
- Configure SQLite database
- Define data models: `Pot`, `Member`, `Drop`, `Split`
- Set up Django admin for all models
- Configure static files (HTMX and TailwindCSS via CDN)
- Basic URL routing skeleton
