# Phase 9.2 — Definition of Done

- `/pot/<token>/manifest.json` returns a web app manifest with the pot's name and start_url
- `/sw.js` serves a minimal service worker enabling browser install
- Pot detail page includes manifest link and Apple meta tags
- Adding to home screen via browser menu opens directly to the pot page
- Help page mentions the browser menu → Add to Home Screen tip
