# Phase 17 — Definition of Done

- [x] Home → pot → back → Home (not stuck on pot)
- [x] Home → pot → "↑ Home" → Home → back → does not return to pot
- [x] pot → Add Drop → save → pot (new drop visible) → back → Home
- [x] pot → Add Drop → save with validation error → stays on form, no navigation
- [x] pot → drop → Edit → save → drop (updated) → back → pot → back → Home
- [x] pot → drop → Edit → save with validation error → stays on form, no navigation
- [x] pot → drop → Delete → pot (drop gone) → back → Home (not the deleted drop)
- [x] pot → Settings → save name → pot (name updated) → back → Home
- [x] pot → Settings → add placeholder → stays on settings, list updated, input cleared
- [x] pot → Settings → ping bot → stays on settings, flash message shown
- [x] Home → list → add item → list updated in place, input cleared → back → Home
- [x] Home → list → toggle/delete item → list updated in place → back → Home
- [x] list → edit item → save → list (updated) → back → Home
- [x] list → settings → rename → list (name updated) → back → Home
- [x] Home → New Pot → create → pot → back → Home (new pot visible)
- [x] Home → New List → create → list → back → Home (new list visible)
