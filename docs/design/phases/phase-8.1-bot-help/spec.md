# Phase 8.1 — Bot /help Command

> Give new users a quick reference without leaving Telegram.

## Command

`/help` — replies with a list of all available bot commands and their usage.

## Tasks

- Add `/help` command handler
- List all commands: `/pot`, `/link`, `/drop`, `/balance`, `/settle`
